package com.paresh.practice.bfs;

public class SnakesAndLadders {

    public int snakesAndLadders(int[][] board) {
        int chances = 0;

        int rows = board.length ;
        int cols = board[rows].length;

        for ( int i = rows -1 ; i >=0 ; i--){
//            if()
        }
        return chances;
    }
}
